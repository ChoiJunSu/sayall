doctype html
html
    head
        title= SAYALL
        a(href="/") SAYALL
        br
        link(rel="stylesheet", href="/stylesheets/style.css")
    body
        p <회원가입>
        form(action="/auth/register", accept-charset="utf-8", method="post")
            p 아이디:
            input(name="id", type="text", required)
            p 비밀번호:
            input(name="pw", type="password", required)
            p 이름:
            input(name="name", type="text", required)
            p 전화번호:
            input(name="phoneNumber", type="tel", placeholder="-는 생략해주세요.", required)
            p 이메일
            input(name="email", type="email", required)
            p 닉네임
            input(name="nickname", type="text", required)
            button(type="submit") 회원가입
            button(type="button", onclick="window.history.back()") 취소