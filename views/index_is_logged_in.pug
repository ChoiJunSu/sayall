extends layout

block content
    - console.log(user);
    p 안녕하세요 #{user.name} 님
    buttone(onclick="location.href='/auth/logout'") 로그아웃
    button(onclick="location.href='/company/search'") 회사 검색
    button(onclick="location.href='/user/my_request'") 내 요청
    button(onclick="location.href='/user/my_profile'") 내 프로필

