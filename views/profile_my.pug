extends layout

block content
    if profiles.length == 0
        p 프로필을 생성해주세요
    else
        ul
            each profile in profiles
                li 회사 이름: #{profile.companyName} 직급: #{profile.position} 입사일: #{profile.startDate} 퇴사일: #{profile.endDate}
    button(onclick="location.href='/profile/register'") 프로필 추가